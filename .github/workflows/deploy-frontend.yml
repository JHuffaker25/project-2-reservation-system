name: Deploy Frontend to S3

on:
  push:
    branches:
      - main
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          VITE_API_BASE_URL: http://Group1-hotel-dev-env.eba-fqsp3cug.us-east-1.elasticbeanstalk.com
          VITE_STRIPE_PUBLIC_KEY: ${{ secrets.STRIPE_PUBLIC_KEY }}
          VITE_STRIPE_PRIVATE_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
          VITE_GOOGLE_LOGIN_URI: http://Group1-hotel-dev-env.eba-fqsp3cug.us-east-1.elasticbeanstalk.com/oauth2/authorization/google

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Deploy to S3
        run: |
          if [ "${{ github.ref_name }}" = "main" ]; then
            aws s3 sync dist/ s3://project2-frontend-group1-397345411365-useast1
          elif [ "${{ github.ref_name }}" = "dev" ]; then
            aws s3 sync dist/ s3://project2-frontend-group1-dev-397345411365-us-east-1
          fi
